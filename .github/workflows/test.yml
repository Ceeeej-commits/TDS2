name: Test Workflow

on: 
  push:
    branches:
      - main  # or any branch you want to trigger the action

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: 25ds1000057@ds.study.iitm.ac.in
        run: echo "Hello, world!"
